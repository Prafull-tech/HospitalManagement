# Copy this file to application-local.yml (in backend/ or same dir as application.yml)
# and set your MySQL credentials. application-local.yml is gitignored.
#
# spring:
#   datasource:
#     username: root
#     password: your_mysql_password

spring:
  datasource:
    username: root
    password: Maknish@2217

hms:
  tenant:
    base-domain: hms.local
    alias-base-domains: hms.com
    dev-default-subdomain: genius36
  security:
    jwt:
      expiry-minutes: 30
      refresh-expiry-minutes: 30