package com.hospital.hms.system.entity;

/**
 * Whether a module is visible, hidden, or read-only for a role. DB-agnostic enum.
 */
public enum ModuleVisibility {
    VISIBLE,
    HIDDEN,
    READ_ONLY
}
